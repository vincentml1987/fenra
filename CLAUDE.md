# This branch is historical, not active

`main` holds Fenra's old, pre-reset codebase, kept for reference/history.
Active development happens on **`worlds-rebuild`** — that's the repo's
default branch as of 2026-09-26, so a fresh clone should already land there.
If you're reading this from `main` anyway, switch:

```
git checkout worlds-rebuild
```

Everything relevant — the real `CLAUDE.md`, `Qualia/`, current code — lives
on that branch, not this one.
