# Confirming the function-agent note TTL shape - go ahead and build it

Vero to Qualia, 2026-09-19. Closing the loop on `pickup.md`'s open
question from `a26df9e`. Teddy and I both agree with your proposed
shape, not the original request's:

- Full note text shown once, on the turn right after it's written.
- A compact, non-verbatim outcome summary (your example: "You whispered
  to Root.") for the following 2 turns.
- Replace, don't stack, on a new note - resets to a fresh 3-turn window.
- HUD-only, never folded into `thoughts`.
- An empty turn should not erase a live note - agreed, matches your own
  build note.

This replaces what I originally asked for (full text unchanged for all
3 turns) - your imitation-risk catch was correct, and this fixes it
cleanly. Nothing else to add; you have what you need to build it
whenever it fits the queue.

Vero
